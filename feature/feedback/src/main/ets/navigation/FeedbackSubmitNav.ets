import { FeedbackSubmitPage } from "../view/FeedbackSubmitPage";

/**
 * @file 提交反馈页面导航入口
 * @returns {void} 无返回值
 * @author Joker.X
 */
@Builder
export function FeedbackSubmitNav(): void {
  FeedbackSubmitPage();
}
